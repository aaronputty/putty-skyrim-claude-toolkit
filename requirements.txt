# esplugin removed — no Python package exists on PyPI (it's a Rust crate).
# examples/inspect-esp.py handles the missing import gracefully; Spriggit
# covers record inspection. Revisit if Python bindings are ever published.
pydantic==2.6.0
cattrs==23.2.0
PyYAML==6.0.1
pytest==8.0.0
pytest-cov==4.1.0
ruff==0.3.0
mypy==1.9.0
